<?php

/*
 * Explicit mapping between Settings outlet IDs (string, used in menu_item_outlets / outlets.id)
 * and numeric outlet IDs (used on orders, GRN, inventory_stocks).
 *
 * Do not guess: only keys listed here are bridged. Configure per deployment.
 *
 * Example:
 *   'by_key' => [
 *       'main-branch' => 1,
 *   ],
 */
return [
    'by_key' => [],
];
